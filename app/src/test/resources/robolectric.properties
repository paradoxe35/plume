# Robolectric runs the real Android framework jar, and the SDK 36 jar requires a Java 21 toolchain.
# The build targets Java 17, so tests run against SDK 34 instead. Nothing under test here is
# version-sensitive: ACTION_PROCESS_TEXT and its extras have been stable since API 23.
sdk=34
